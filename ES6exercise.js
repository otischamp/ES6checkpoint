const products = [
    { id: 1, name: 'Laptop', price: 999.99 },
    { id: 2, name: 'Phone', price: 699.99 },
    { id: 3, name: 'Headphones', price: 99.99 }
  ];

  
products.map((param) => {console.log(param)})



// (param) => {return param};

// console.log(products.find((param) => {return param}));

// console.log(products.map((array) => {return array.name}));

// console.log('this is products:',products.map((param) => {return param}));